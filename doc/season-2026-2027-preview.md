# 2026–2027 赛季与前瞻

## 赛季范围与当前页面

`core.seasons.SEASON_2026_2027` 定义 `2026-2027` 系列，候选比赛来自 RankLand official collection 的 `icpc2026` 与 `ccpc2026`，沿用邀请赛排除、显式 include/exclude 覆盖和同日 CCPC 优先的排序规则。公开 API 为 `SEASON_2026_2027` 与 `load_2026_2027_season`。

当前已发布“2026 ICPC Asia EC网络预选赛 - 第一场”的选手 Rating，同时保留该场赛前前瞻，并加入第二场前瞻。站点索引在同一条记录提供 `path`、`previewPath`、`previews` 与 `reviewPath`；本赛季尚未发布题目难度数据。复盘规则、结果关联及全指标符合度对比见 [赛后复盘](post-contest-review.md)。

2026-09-06 从 RankLand 获取 `icpc2026preliminary-1`，比赛时间为北京时间 13:00。上游共 2535 支正式队伍，排除 39 支无提交队伍后，2496 支队伍的 7380 个规范化选手身份参与计算。新赛季从空状态、初始 Rating 1400 开始，不继承上赛季评分；首次发布的最终 Rating 范围为 1334–1720。沿用学校与姓名身份规范化、成绩重建排名和重复身份取最好排名政策。

上游 SRK 文件 ID 为 `90036200580141056`，SHA-256 为 `39077e4904169b353f85c4484efa5fe9064727ba00cf6685661270d5d9bf7575`。发布文件为 `static/data/series/2026-2027.json`，默认生成器已接入 `load_2026_2027_season`；后续重新生成使用当时的实时上游数据。离线测试覆盖生成器的新赛季加载、已发布首场数据的 1400 初始值与 Rating 算术，以及索引同时包含 Rating 和前瞻入口。

## 名单来源与更新边界

第一场 2535 支队伍来自 [ICPC 报名系统队伍公示](https://uep.pintia.cn/icpc-reg/examGroups/2086678069855703040/publicTeams) 在 2026-09-04 的静态快照。只读取学校、中文队名和“队员”列，明确排除教练列。

前瞻名单不是 Pintia 比赛榜单，不包含比赛过程或成绩。浏览器只读取仓库内已提交的 `static/data/previews/` 快照，不会请求报名系统，也不会随报名、审核或比赛进程自动变化。后续新增场次、替换名单或刷新某个来源，均只在用户明确指定后执行；`scripts/generate_static_data.py` 只复制已提交快照，不联网刷新它。

第二场来自 [第二场队伍公示](https://uep.pintia.cn/icpc-reg/examGroups/2086691546024431616/publicTeams) 在 2026-09-07 的快照，共 2636 支队伍、7788 名队员，沿用公示顺序并排除教练。场次 ID 为 `icpc-2026-preliminary-2`，文件为 `static/data/previews/icpc-2026-preliminary-2.json`。官方 exam group 的时间为北京时间 2026-09-12 13:00–18:00，`sortAt` 记录开赛时间。2026-09-07 按用户要求刷新第二场的 XCPC Rating 与 XCPC Elo：分别使用线上发布数据的 `2026-09-06T16:17:41.538314+00:00`（北京时间 9 月 7 日 00:17）与 `2026-09-07T04:26:01.864Z`（北京时间 12:26）版本，两者均已纳入第一场比赛。当前四项外部/上赛季评分分别匹配 7189、7155、3007、2241 人；六维综合战力随评分重新计算。上赛季最后比赛 2026-04-26、CPC Finder 2026-09-04、NOI/IOI 缓存及本赛季 Rating 保持原样；第一场前瞻保留赛前评分，不回填赛后数据。

本次 XCPC Rating 来源为 [线上数据元信息](https://hei-maom.github.io/xcpcrating/data/meta.json) 和 `data/leaderboards/official/pages/1.json` 至 `623.json`，校验正式榜总计 62261 条记录，并在下载后复核元信息未改变；仓库 `main/web/public/data` 仍是旧版，不能代替线上最新发布。XCPC Elo 来源为 [线上 data.js](https://zzzzzzyt.github.io/xcpc-elo/data.js)，当前共 66573 名选手。XCPC Rating 缓存保存在 `data-cache/preview-sources-20260907/`，最新 Elo 缓存保存在 `data-cache/preview-sources-20260907-elo-refresh/`。发布前验证仅这两个评分、来源时间与匹配计数发生变化，名单和其他维度逐项一致；离线快照测试校验更新时间、覆盖人数与完整评分聚合契约。

XCPC Elo 在 2026-09-07 再次按用户要求从 10:46 版本刷新至 12:26 版本：第二场 7175 个队员评分条目、2473 支队伍的 Elo 最高分变化，1458 支队伍的六维综合战力名次变化；匹配人数从 7197 降至 7155。新版本仍截至第一场比赛，未匹配者按既有规则保留空值。此次仅更新 `xcpcElo` 的成员/队伍评分、来源时间和匹配计数，逐项验证其余字段一致，并验证第一场快照字节未变。离线回归更新了 Elo 来源时间与覆盖人数，并通过完整前瞻 schema 和聚合校验。

`scripts/generate_preview_data.py` 支持 `--preview-id`、`--preview-title`、`--team-source-url`、`--sort-at`，默认仍为第一场。第二场显式传入上述元数据和独立 `--output` 路径。`--cpcfinder-snapshot-date` 保留缓存来源日期，省略时沿用 `--snapshot-date`；名单与评分日期可以不同，均不表示实时数据。离线测试覆盖参数传递、缓存日期、教练排除、默认发布两场快照、完整快照校验及第二场 URL 选择。

第二场通过 `--current-series static/data/series/2026-2027.json` 增加 `currentSeason` 评分来源，标题为“本赛季 Rating”，位于 `previousSeason` 后、CPC Finder 前。该可选参数省略时仍生成原有四项评分。输入必须属于本赛季，且最后比赛时间严格早于前瞻开赛时间，避免将赛后结果用于赛前评分；本次来源截至 2026-09-06 13:00，匹配 6674 名队员。仍按姓名与学校匹配、队内取最大值，未匹配保留空值，不补初始分。该来源日期记录在 `sourceSnapshots.currentSeason`；后续评分更新不会自动修改静态快照。离线测试覆盖可选参数、来源赛季与时间校验、成员匹配及聚合、六维战力排名、并列、缺失值及列顺序。

## 数据来源与聚合

第一场每名队员记录四项评分；第二场额外记录本赛季 Rating：

- [Hei-MaoM/xcpcrating](https://github.com/Hei-MaoM/xcpcrating) 的正式参赛榜 Rating；
- [Zzzzzzyt/xcpc-elo](https://github.com/Zzzzzzyt/xcpc-elo) 的选手 Elo；
- 本仓库 `2025-2026` ICPC + CCPC series 的最终 Rating；
- 第二场在上赛季 Rating 后加入本仓库 `2026-2027` series 的本赛季 Rating；
- [CPC Finder](https://cpcfinder.com/) 的选手评分。

队伍表每个评分单元格显示该项已匹配队员中的最高值，并在同一行以 `#x` 显示该值在全部报名队伍中的名次；各数据列为小号等宽数字排名保留相同的紧凑窄槽，主值右对齐，排名在窄槽内左对齐并紧随主值。同值使用竞赛排名并列，后续名次留空位，没有任何匹配时显示 `—` 且不显示名次。CPC Finder 在队伍单元格及队员悬浮明细中按原值显示：`0` 显示为 `0.00`，缺失值显示为 `—`。零分参与该列竞赛排名并显示名次，同分并列；评分排序按数值比较，缺失值始终置后且无名次。仅综合战力将零分和缺失值视为该维度同一最弱档，互不超过。回归测试覆盖 CPC Finder 整数、零分、小数的两位格式及缺失值展示、零分排名与并列、评分双向排序及缺失值置后、原值展示路径、零分与缺失值的战力计数均为 0，以及正分同时超过两者。Rating 不使用千分位，XCPC Rating 与 CPC Finder 始终保留两位小数（队伍值与队员明细一致）。XCPC Rating 使用其深牛津蓝视觉，XCPC Elo 使用其等级色与橙色定义，其中 `>=3000` 的首位数字为黑色、其余数字为红色；上赛季与本赛季 Rating 均沿用本站选手 Rating 的 Codeforces 风格等级色；CPC Finder 保持普通文本色。鼠标正常悬浮或键盘聚焦单元格会展示所有队员在该来源的值，界面不显示额外提示图标，未匹配成员仍显示 `—`。金、银、铜牌数来自 CPC Finder，按队员求和为队伍总数；奖牌表头只显示“奖牌”，单元格仅以空白分隔三个图标及数值，排序和名次均依次比较金牌、银牌、铜牌。

综合战力按场次的 `metricSources` 加奖牌计算：第一场四项 Rating 加奖牌共五维，第二场五项 Rating 加奖牌共六维。对每支队伍、每个维度统计其严格超过的队伍数；评分缺失值作为该维度最弱值处理，两个缺失值互不超过，奖牌维度按金、银、铜字典序比较。把同一队各维度的计数从高到低排列为元组，再按元组字典序对全部队伍排序；完全相同的元组并列。综合战力列显示该全局名次，悬浮或聚焦时只显示按各原始维度归一化的雷达图（第一场五边形、第二场六边形），不展示内部排序元组。搜索和学校筛选不会重新计算全局战力。

## 身份匹配

匹配严格执行“先人名、后学校”：先按 `DefaultNormalizer.member` 规范化姓名查找候选，再要求 `DefaultNormalizer.school` 规范化后的学校完全相同。学校规范化会做 NFKC、繁简转换、大小写与标点清理，并使用 `xcpc-standings/data/config/school.json` 的中英文别名。不会因为某姓名在来源中只有一个候选就忽略学校；校名仍无法确认时保留为空值。

同一来源若残留多个相同规范化姓名与学校的历史身份，确定性选择评分最高的记录。该策略服务于本页面“队内最高分”口径，并避免重复累计 CPC Finder 奖牌。第一场 7485 名报名队员中，四项来源分别匹配 4812、5085、2959、2186 人；覆盖差异主要来自各系统收录范围和新人，不代表零分。

NOI 与 IOI 履历仅按原始姓名完全匹配，不要求学校一致。成员姓名下方依次使用 NOI、IOI 最佳奖牌颜色的两条线；悬浮或聚焦姓名时按年份展示履历，同年 NOI 排在 IOI 前。每条获奖记录显示为“比赛名 · 奖牌#名次 · 分数 / 总分”；NOI 名次按同届加分后总分使用竞赛排名计算，当前收录年份总分均为 705，IOI 使用该届数据记录的总分。沈吉滪另记录 IOI 2024 参赛未获奖，保留黑线，履历仅显示“2024 IOI”，不显示参与、未获奖、名次、分数或分隔符。所有仅参赛的 IOI 记录沿用该展示规则；回归测试覆盖文本格式和渲染行仅含比赛名称。

## 静态契约、限制与测试

第一场的标题下方显示“比赛时间：2026年9月6日 13:00-18:00”，时间按用户指定的北京时间以静态文案呈现，不做浏览器时区转换；第二场根据 `sortAt` 按北京时间显示 2026年9月12日 13:00。

前瞻 schema v1 包含系列/场次标识、快照日期、名单来源、评分来源、匹配政策和 `teams[]`。每队含稳定 ID、来源顺序、学校、队名、成员明细、各来源的队内最大评分与奖牌总数。校验器检查来源 ID、队伍 ID、评分有限性、队内最大值及奖牌求和；筛选支持学校、队名和全体成员姓名；缺失评分始终排在有值队伍之后。

当前快照约 1.7 MB，一次性加载后以虚拟滚动表显示。页面不展示内部快照日期、匹配政策或静态快照说明，名单来源与各项数据来源分成两行，并批注学校排名随当前排序方式变化，以及 NOI/IOI 成绩仅按姓名匹配、可能存在重名情况。任意列的主排序键相同时，均按学校名升序排列，再按来源顺序保持确定性。除学校、队名和成员排序外，先对未筛选的全体队伍应用当前排序，再按各校最先出现队伍的主排序键计算竞赛排名；主排序键相同的不同学校共享校排，后续名次按此前学校数跳号。同一学校有多个并列队伍时，只在其中最前面的队伍校名前显示 `#x`，其余队伍留空。搜索和学校筛选只裁剪全量排序结果，不重算校排，因此筛出的队伍若不是原结果中的校内最高队伍则不显示校排。“赛前前瞻”始终作为独立 tab 显示，即使当前系列没有其他页面；标题区保留比赛选择下拉框，当前按第一场、第二场排列，通过索引可选 `previews: [{id, path}]` 扩展场次，兼容现有 `previewPath`，`previewContest` URL 参数记录选择。所有前瞻列表头文字居中。各项 Rating 和奖牌名次以全部队伍为基数预先计算，搜索和学校筛选不会重新排名。桌面容器宽于 `1120px` 时冻结学校、队名和成员三列，`600px` 到 `1120px` 冻结学校和队名；`600px` 以下只隐藏独立学校列，把队名作为主行、学校作为副行，校排仍以 `#x 学校` 的形式直接放在校名前，合为 `clamp(150px, 42cqw, 176px)` 的单一冻结身份列。成员列继续显示但不冻结，随 Rating 数据横向滚动；移动端身份列右侧使用阴影提示横向滚动，点击或键盘聚焦队伍身份也可查看成员名单。身份列表头显示“队伍”并继续按队名排序。表格总宽从当前可见列计算，防止浏览器重新分配列宽。成员明细与综合战力悬浮窗固定到视口并根据剩余空间显示在单元格上方或下方，避免被表格滚动容器裁切。它不是比赛结果预测，不给未匹配选手填新人先验，也不承诺外部评分体系彼此可比；综合战力仅是上述各维度相对支配计数的确定性排序。测试覆盖新赛季 collection、前瞻 schema、真实 2535 队快照、搜索、各列排序及学校名次级排序、各数据列全局名次、综合战力五维/六维元组、本赛季评分并列与缺失值、校排并列、奖牌关键字顺序、缺失值顺序、URL 状态、子路径加载与单列移动冻结布局。

金、银、铜牌在赛前前瞻、赛后复盘及成员悬浮明细中共用固定网格：每种奖牌的图标占 1em、数量占 2ch，三组依次排列。图标居中、数字右对齐并使用等宽数字，不随一位或两位数移动；当前快照的最大数量分别为 35、24、17。奖牌顺序和排序规则保持不变，浏览器回归检查两页及移动端的各组位置一致。
