name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "static/**"
      - "tests/*frontend_data.mjs"
      - "tests/fixtures/review/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test frontend data utilities
        run: node --test tests/test_frontend_data.mjs tests/test_problem_rating_frontend_data.mjs tests/test_preview_frontend_data.mjs tests/test_review_frontend_data.mjs

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: static

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
